# 一个人的小网站# 我的个人项目
## 首页
<!-- 待开发 -->
## 1关于我
- 准备取消
## 2我的项目-我的自定义功能区 需要什么自己搓
- [ ] new tap
## 3我的精选照片-更名为《照片墙》
- 已取消
<!-- ### 基本框架已经实现，但部分体验过程需要优化、改进
- [ ] 画布大小过小，需要扩大[2026年8月18日 星期二]        已改进，但无效果
- [x] 导入/导出功能没用，建议取消[2026年8月18日 星期二]
- [ ] 1.清除“小贴士”部分，改为功能区
     - [x] 返回首页
     - [ ] 升级为管理员 **管理员可以修改/提交如片，普通用户只能浏览**
- [x] 原本底部的提示弹窗移动至页面上方[2026年8月18日 星期二]
- [x] 将顶部提示弹窗改为圆角[2026年8月18日 星期二]
- [ ] 将顶部的提示弹窗改为渐入渐出 或 上浮进入
- [ ] 取消修改墙面背景，将墙面背景改为固定颜色为 #fff5c0
- [ ] 拖动墙面背景时，移动
- [ ] 添加一些图标元素
- [ ] 被管理员提交的图片被永久保留到github -->
## 4联系我
- [x] 4.1 微信联系方式
- [x] 4.2 QQ联系方式
- [ ] 4.3 小红书联系方式
- [ ] 4.4 抖音联系方式

## 我的空间 myspace.html
- [x] 导航栏仍然保留毛玻璃色，蓝色选项卡，背景选用黄色调
- [x] 在左侧大约 200px 的位置添加“介绍”卡片 大小约为 300px*200px 
    - [x] 我们直到失去才知道自己拥有什么。 
        We don't know what we've got until we lose it.
- [ ] 在介绍卡片的下方添加“信息”卡片
    - [ ] 搜索我的空间
    - [ ]在“添加”卡片中添加'添加’按钮
        - [ ] 在“添加”卡片中添加

## 网页库
- [x] 整体样式修改
- [x] 弹窗细节优化
- [x] 新增添加文件夹
- [x] 新增文件夹重命名
- [x] 内容可自由移动
- [x] 每个链接/文件夹后有“三点”入口，长按拖动三点可自定义排序并持久化
- [x] 新建文件夹后名称前自动添加 📁 图标
- [x] 点击文件夹进入层级：清空当前内容并显示该文件夹内的链接与子文件夹
- [x] 三点菜单新增“移动至...”：可将链接/文件夹移动到任意目标文件夹或根目录
- [x] 预置链接移动后写入本地存储，刷新页面不丢失
- [x] 移动URL位置时变丝滑一些
- [x] 增加左侧导航栏

## 图片添加水印工具
<!-- - 纯前端单文件应用（HTML/CSS/JS 全部内联），所有处理在浏览器本地完成，无任何后端，图片不上传
- [x] 两种处理模式：单张处理 / 批量处理，顶部一键切换，切换后各自已加载数据不丢失
- [x] 图片上传：点击或拖拽，支持 JPG / PNG / WebP，单张 ≤ 20MB；批量模式支持多选（单次最多 30 张）
- [x] 文字水印：内容 / 字号（8-200pt，默认 48）/ 颜色（#RRGGBB，拾色器与十六进制输入框双向联动，非法输入回退上次合法值）
- [x] 图片水印：上传水印图片（推荐透明底 PNG），尺寸占原图宽 5%-50%（默认 20%），可移除
- [x] 透明度 0-100%（默认 80%），文字与图片水印均适用
- [x] 旋转角度 -180° ~ 180°（默认 -30°），水印绕自身中心整体旋转
- [x] 水印位置：九宫格选择（默认右下）+ 自由拖拽（3px 启动阈值防误触，按旋转后包围盒钳制不越界，选中九宫格后仍可继续拖动）
- [x] 平铺模式：水印按网格铺满整张图（单元间距 = 水印尺寸 × 2.0，与位置/拖拽互斥）
- [x] 实时预览：画布宽 ≤ 1200px，参数变更 80ms 防抖合并重绘
- [x] 原尺寸导出（不受预览缩放影响），格式 PNG / JPG（JPG 质量 0.92，透明区白色铺底）
- [x] 导出文件名：原文件名_水印.新扩展名（拿不到原文件名时兜底 watermark_图片）
- [x] 批量任务列表：缩略图 + 状态角标（待处理/处理中/成功/失败），支持单条移除、整批清空
- [x] 批量打包下载：ZIP 压缩包（watermark_batch_yyyyMMdd_HHmmss.zip），包内重名文件自动加 _1/_2 序号，任一步失败即中止、不残留半成品
- [x] JSZip 本地化 jszip.min.js（禁用 CDN），页面打开即异步预加载，加载失败仅降级批量打包、单张导出不受影响
- [x] 浏览器能力检测（Canvas / 2d / createObjectURL / toBlob），不支持时显示降级提示条并禁用上传/导出
- [x] Toast 轻提示（success / error / info，最多堆叠 3 条，默认 2.4s 自动消失）
- [x] 统一渲染管线：预览 / 批量缩略图 / 单张导出 / 批量打包四处共用同一绘制入口，保证结果一致
- [x] 顶部导航栏与全站共享样式一致；移动端（<900px）单列堆叠
- [x] Blob URL 统一管理，替换/移除图片时及时 revoke，防内存泄漏
- 参数区间（字号/透明度/尺寸/批量上限等）统一收敛在文件内 JS 的 CONFIG 常量区，调参只改那一处
- 依赖：页面同目录下的 jszip.min.js（批量打包用） -->
<!-- - [ ] 顶部导航栏同步添加该工具 -->
- [ ] 在index.html 里添加“我的小工具”
     - [ ] 放置在“我的项目”下方 简介height缩短 “我的项目”上调
     - [ ] 取消“日程” 并将“我的小工具”替换

- [x] 左侧功能调节的工具设为导航栏 右侧固定不动
- [x] 用户添加/上传图片的位置放置在左侧导航栏
- [ ] “单张处理”与“多张处理”合二为一 由系统自动识别
- [x] 图片文字字体
- [x] 平铺模式的按钮点不了的问题
- [ ] 水印位置自定义
- [ ] 导航栏在开启前默认关闭
- [ ] 